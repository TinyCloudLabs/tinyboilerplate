# Backend environment — copy to .env and fill in values
# Generate a private key: bun run generate-key (from repo root)
BACKEND_PRIVATE_KEY=0x...
TINYCLOUD_HOST=https://node.tinycloud.xyz
OPENKEY_ISSUER_URL=https://openkey.so
PORT=3001
