# Required. Generate a key from the repo root with `bun run generate-key`,
# then paste the BACKEND_PRIVATE_KEY value here.
BACKEND_PRIVATE_KEY=0x...

# Optional backend settings. The defaults below match a fresh checkout.
TINYCLOUD_HOST=https://node.tinycloud.xyz
FRONTEND_URL=http://localhost:5173
PORT=3001
