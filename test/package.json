{
  "name": "tinyboilerplate-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "app-shell-smoke": "bun app-shell-smoke.ts",
    "install:browsers": "playwright install chromium",
    "install:browsers:ci": "playwright install --with-deps chromium",
    "real-auth": "bun real-auth-manual.ts"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
